const express = require("express");

const app = express();

const products = [];

/**
 * POST => Insert data;
 * GET => Search one or more data;
 * PUT => Change a data;
 * DELETE => Remove a data; 
 **/

app.post("")

app.listen(4002, () => console.log("Servidor está rodando na porta 4002"))
